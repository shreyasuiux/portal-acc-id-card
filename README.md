
  # ACC ID Cards

  This is a code bundle for ACC ID Cards. The original project is available at https://www.figma.com/design/Um2hlxlkYmNHarOGvaasfD/ACC-ID-Cards.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # portal-acc-id-card
