# Netlify SPA Routing Configuration
# This file ensures all routes are redirected to index.html
# Fixes "Page Not Found" errors when refreshing /dashboard or other routes

# Redirect all paths to index.html (SPA fallback)
/*    /index.html   200
